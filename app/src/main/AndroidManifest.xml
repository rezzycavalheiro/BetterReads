<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.betterreads">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LavaCarApp">
        <activity android:name="com.example.betterreads.view.Profile"></activity>
        <activity android:name="com.example.betterreads.view.StartMenu" />
        <activity
            android:name="com.example.betterreads.view.PopUpInfo"
            android:theme="@style/PopInfo" />
        <activity android:name="com.example.betterreads.view.OrderSent" />
        <activity android:name="com.example.betterreads.view.OrderPage" />
        <activity android:name="com.example.betterreads.view.CameraPage" />
        <activity android:name="com.example.betterreads.view.Register" />
        <activity android:name="com.example.betterreads.view.Login" />
        <activity android:name="com.example.betterreads.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.betterreads.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/fileprovider" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>